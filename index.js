function remainder(a, b) {
  return a % b;
}

console.log(remainder(3, 4));

const firstIndex = (arr) => {
  return arr[2];
/// };

const firstArray = [1, 2, 3, 4, 5, 6];

console.log(firstIndex(firstArray));
